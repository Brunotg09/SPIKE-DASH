name: spike_dash_app
description: "Arena de Reflexos e Ritmo - SPIKE DASH"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.6 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  flutter_dotenv: ^5.2.1

  # --- GERENCIAMENTO DE ESTADO ---
  provider: ^6.1.1

  # --- BANCO RELACIONAL LOCAL (SQLite) ---
  sqflite: ^2.3.0
  path: ^1.8.3

  # --- BANCO NOSQL LOCAL (Hive) ---
  hive: ^2.2.3
  hive_flutter: ^1.1.0

  # --- FIREBASE (Nuvem) ---
  firebase_core: ^3.12.1
  cloud_firestore: ^5.6.5
  firebase_auth: ^5.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

  # --- GERADOR DE CODIGO HIVE ---
  hive_generator: ^2.0.1
  build_runner: ^2.4.7

flutter:
  uses-material-design: true
  assets:
    - .env
